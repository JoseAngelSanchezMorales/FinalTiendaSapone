<%- include('../components/html/header.ejs') %>
</head>

<body>
    <%- include('../components/html/nvadmin.ejs') %>

    <div class="container mt-5">
        <div class="row">
            <div class="col">
                <div class="form-group">
                    <input type="text" name="email_contact" id="email_contact" class="form-control"
                    value="<%= message.email_contact %>" disabled>
                </div>
                <div class="form-group">
                    <input type="text" name="name_contact" id="name_contact" class="form-control"
                        value="<%= message.name_contact %>" disabled>
                </div>
                <div class="form-group">
                    <textarea name="message" id="" rows="5" class="form-control" disabled><%= message.message %></textarea>
                </div>
            </div>
            <div class="col">
                    <div class="form-group">
                            <textarea name="message" id="" rows="5" class="form-control" placeholder="Your message"></textarea>
                    </div>
                <form action="/answered/<%= message.id %>" method="POST">
                    <button type="submit" class="btn btn-primary">contestar</button>
                </form>
            </div>
        </div>
    </div>


    <%- include('../components/html/tail.ejs') %>